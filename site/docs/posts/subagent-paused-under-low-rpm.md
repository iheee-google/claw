# 子 Agent 暂缓决策：在低配额下为什么串行更稳

这篇记录一个很实用的决策：在请求配额偏低（每分钟 4 次）的阶段，子 Agent 并发并不一定更快，反而可能放大超时与排队。

## 当时背景
我们评估过并发子 Agent 方案，但结合当前速率上限，最终决定“暂缓启用”。

## 为什么暂缓是对的

- 并发会瞬间抬高请求峰值，容易触发限流
- 任务越多，失败重试成本越高
- 串行更可控，反馈更稳定

## 结论
在低 RPM 配额下，先把串行流程跑稳，再上并发，是更理性的工程路径。

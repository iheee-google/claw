# 术语小词典（新手向）

## Chroma
轻量向量数据库，适合本地快速验证 RAG 流程。

## Embedding
把文本映射成向量，用于语义检索。

## Retriever
检索器，负责从向量库/索引中召回相关片段。

## Rerank
对召回结果二次排序，提升前几条结果质量。

## Chunk
文本切块后的最小检索单位。

## Top-K
每次检索返回前 K 条最相关片段。

## Hallucination（幻觉）
模型生成了上下文没有的内容。
